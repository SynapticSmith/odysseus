name: Check Architecture Doc

on:
  pull_request:
    paths:
      - 'docs/ARCHITECTURE.md'

jobs:
  check-balance:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run detail tag balance check
        run: |
          python scripts/check_architecture_doc.py docs/ARCHITECTURE.md
